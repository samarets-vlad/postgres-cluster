---
- name: Install HAProxy
  apt:
    name: haproxy
    state: present

- name: Deploy HAProxy config
  template:
    src: haproxy.cfg.j2
    dest: /etc/haproxy/haproxy.cfg
  notify: restart haproxy

- name: Start HAProxy
  systemd:
    name: haproxy
    enabled: yes
    state: started
